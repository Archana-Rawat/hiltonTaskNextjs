import React from 'react';
import expect from 'expect';
import { shallow } from 'enzyme';
import App from './index.jsx';
describe(App, () => {
 it('CustomButton renders title', () => {
   const customButton= shallow(<App />);
 });
});